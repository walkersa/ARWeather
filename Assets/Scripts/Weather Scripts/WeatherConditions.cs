using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum WeatherConditions : int
{
    clear_sky = 0,
    few_clouds,
    scattered_clouds = 2,
    broken_clouds,
    shower_rain,
    rain = 1,
    thuderstorm,
    snow,
    mist
}
